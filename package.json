{
  "name": "rx0",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "rx0 code navigator: browser UI build and test toolchain",
  "scripts": {
    "build": "node scripts/sync-web-vendor.js && rsbuild build",
    "test": "rstest run",
    "lint": "rslint web/src",
    "desktop:dev": "sh scripts/prepare-sidecar.sh debug && tauri dev",
    "desktop:build": "sh scripts/prepare-sidecar.sh release && tauri build"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.2.7",
    "@rslint/core": "^0.9.2",
    "@rstest/core": "^0.12.0",
    "@tauri-apps/cli": "^2.11.4",
    "@xterm/xterm": "^6.0.0",
    "globals": "^17.12.0",
    "happy-dom": "^20.14.5"
  }
}
